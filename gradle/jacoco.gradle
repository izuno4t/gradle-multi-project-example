apply plugin: 'jacoco'

jacoco {
//    https://github.com/jmockit/jmockit1/issues/615
//    toolVersion = "0.8.5"
    toolVersion = "0.8.3"
}

jacocoTestReport {
    reports {
        xml.enabled false
        csv.enabled false
        html.destination file("${buildDir}/jacocoHtml")
    }
}
