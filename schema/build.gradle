plugins {
    id "org.flywaydb.flyway" version "7.8.1"
}

flyway {
    url = System.env.POSTGRESQL_URL ?: "jdbc:postgresql://localhost:5432/sample"
    user = System.env.POSTGRESQL_USERNAME ?: "sample"
    password = System.env.POSTGRESQL_PASSWORD ?: "sample"
}
