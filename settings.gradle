include("core")
include("batch")
include("webapp")
include("webservice")

rootProject.name = "gradle-multi-module-example"
rootProject.children.each {project ->
	project.buildFileName = "${project.name}.gradle"
}
